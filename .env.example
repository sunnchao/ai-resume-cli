# Copy to .env in the directory from which you run resume-cli.
# Shell environment variables take precedence. Never commit a real API key.
OPENAI_API_KEY=
OPENAI_MODEL=
# Optional: endpoint must support POST /chat/completions and JSON Schema outputs.
# OPENAI_BASE_URL=https://api.openai.com/v1
